FUNCTION Main()
    PRINT "Hello, Cubasic!"
    DIM arr(10)
    FOR i IN 0 TO 9
        SET arr[i] = i * 2
        NEXT i
    IF arr[5] > 10 THEN
        PRINT "Value is greater than 10"
    ELSE
        PRINT "Value is 10 or less"
    ENDIF
END