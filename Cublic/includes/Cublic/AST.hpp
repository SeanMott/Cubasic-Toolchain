#pragma once

//defines a AST graph

namespace Cube::AST
{
	//defines a AST type
	enum class ASTNodeType
	{
		Identifier = 0,

		Count
	};

	//defines a AST function metadata

	//defines a AST if metadata

	//defines a AST Node
}